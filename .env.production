# Production Environment Variable (Loaded automatically during `npm run build`)
# Empty string allows relative routing /api/ through Nginx reverse proxy (eliminating HTTP/HTTPS Mixed Content block)
VITE_API_BASE_URL=""
